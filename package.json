{
  "name": "treasury-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "build": "npm install && npm run build --prefix frontend"
  },
  "dependencies": {
    "@sentry/vite-plugin": "^5.4.0"
  }
}
